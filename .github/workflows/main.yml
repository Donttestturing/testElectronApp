on:
  push:
    branches:
    - main
jobs: 
  autoDoc:
    runs-on: ubuntu-latest
    steps:
      - name: installJSdocs
        run: npm install -g jsdoc
        
      - name: docFiles
        run: ls -a ${workspace} #jsdoc espose.js #ls -a $HOME/work/testElectronApp/testElectronApp/. 
        
     # - name: moveDocFile
      #  run: cp out/* ~/work/testElectronApp/documentation/ 
    
