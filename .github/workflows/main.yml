on:
  push:
    paths:
    - '**.js'
    - '**.yml'
    branches:
    - main
    
jobs: 
  #autoDoc:
    #runs-on: ubuntu-latest
    #steps:
     # -  name: installJSdocs
     #    run: npm install -g jsdoc
  file_existence:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Check file existence
        id: check_files
        uses: andstor/file-existence-action@v2
        with:
          files: '**.js'

      - name: File exists
        if: steps.check_files.outputs.files_exists == 'true'
        # Only runs if all of the files exists
        run: echo All files exists! ${steps.check_files.outputs}
        






      #-  name: docFiles
      #   run: jsdoc **.js

      #-  name: pdocFiles
      #   run: echo ~/.bash_history
         
         
         
    
    
      
 

        
    #- run: ls -a ${workspace} #jsdoc espose.js 
        
     # - name: moveDocFile
      #  run: cp out/* ~/work/testElectronApp/documentation/ 
    #- name: installJSdocs

